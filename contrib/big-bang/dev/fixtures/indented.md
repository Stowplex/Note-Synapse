# Documenting the format

The block looks like this:

    ```synapse-bigbang
    {"v":1,"items":{}}
    ```

- and inside a list item:
      ```synapse-bigbang
      {"v":1,"items":{"n1":{"k":"note","id":"x","p":[0,0]}}}
      ```

None of the above is a board: only a fence at column 0 counts.
