name: note_synapse
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+12

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # Database
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0
  sqlite3_flutter_libs: ^0.5.0
  path: ^1.8.3
  
  # HTTP requests
  http: ^1.1.0
  crypto: ^3.0.3
  
  # Image handling
  image: ^4.2.0
  image_picker: ^1.0.4
  
  # Voice recording
  permission_handler: ^11.0.1
  
  # Audio recording and playback
  record: ^6.0.0
  audioplayers: ^6.0.0
  
  # Secure storage
  flutter_secure_storage: ^9.1.0
  
  # Alternative secure storage for Linux compatibility
  shared_preferences: ^2.2.2
  
  # State management
  provider: ^6.1.1
  
  # Date handling
  intl: ^0.20.2
  
  # JSON handling
  json_annotation: ^4.8.1
  
  # URL launcher
  url_launcher: ^6.2.1
  
  # Deep links for OAuth redirect handling
  app_links: ^6.0.1
  
  # Sharing
  share_plus: ^12.0.0
  pdf: ^3.11.3
  printing: ^5.13.0
  flutter_pdfview: ^1.3.1
  
  # UUID generation
  uuid: ^4.5.2
  
  # File handling
  file_picker: ^8.0.0
  file_saver: ^0.3.1
  path_provider: ^2.1.1
  open_file: ^3.3.2
  
  # Archive handling
  archive: ^3.4.10
  
  # File text extraction
  # Note: For PDF text extraction, consider adding pdf_text or similar package
  
  # Calendar widget
  table_calendar: ^3.0.9
  
  # Markdown rendering
  gpt_markdown: ^1.1.4
  markdown: ^7.2.1

  flutter_math_fork: ^0.7.3
  
  # WebView for user-defined apps and content extraction
  flutter_inappwebview: ^6.0.0
  
  # MIME detection
  mime: ^2.0.0
  
  # Logging
  logger: ^2.0.2+1
  
  # HTML to Markdown conversion
  html2md: ^1.3.2
  
  # YAML parsing
  yaml: ^3.1.2
  

  
  # Graph visualization for conversation tree
  graphview: ^1.5.1
  
  # MCP (Model Context Protocol) client
  mcp_client: ^1.0.2
  
  # Code editor with syntax highlighting
  re_editor: ^0.8.0
  re_highlight: ^0.0.3

  # Flutter SVG for displaying SVG image
  flutter_svg: ^2.2.1

  # JSON viewer widget
  flutter_json_view: ^1.1.5
  flutter_painter_v2: ^2.1.0+1
  phosphor_flutter: ^2.1.0
  json_repair_flutter: ^2.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  
  # JSON code generation
  json_serializable: ^6.7.1
  build_runner: ^2.4.7
  
  # App icon generation
  flutter_launcher_icons: ^0.13.1

  mockito: ^5.6.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/scripts/Readability.min.js
    - assets/scripts/chart.min.js
    - assets/scripts/chartjs-adapter-date-fns.bundle.min.js
    - assets/scripts/bootstrap.min.css
    - assets/scripts/highlight.min.js
    - assets/scripts/highlight.min.css
    - assets/scripts/mermaid.min.js
    - assets/scripts/d3.min.js
    - assets/scripts/svg.pan-zoom.min.js
    - assets/libraries.yaml
    - assets/model_presets/
    - assets/starter/
    - assets/starter/apps/

  # Internationalization
  generate: true

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/note_synapse_logo.jpg"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "assets/icons/note_synapse_logo.jpg"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "assets/icons/note_synapse_logo.jpg"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/icons/note_synapse_logo.jpg"
